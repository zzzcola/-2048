<!--index.wxml-->
<view class="container">
  	
	<view wx:for-items="{{gameList}}" wx:for-index="idx" wx:key="idx" class="usermotto">
	    <button type="primary" disabled="{{disabled}}" bindtap="start{{item}}"> {{item}} </button>
	</view>

	<text class="more">1!5!</text>
</view>
